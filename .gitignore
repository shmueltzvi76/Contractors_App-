# Dependencies
node_modules/

# Build output (none today — no-build constraint, see docs/handoff-brief.md §4)
dist/
build/

# Environment / secrets — never commit Firebase config or API keys
.env
.env.*
*.local
serviceAccountKey.json

# Editor / OS
.vscode/
.idea/
.DS_Store
*.swp
